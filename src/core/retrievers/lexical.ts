/**
 * BM25. Cheap, deterministic, no network, no key.
 *
 * This is a floor on retrieval difficulty rather than a prediction of agent
 * behaviour, and it flatters keyword-stuffed indexes by design. That is why
 * it is never reported alone.
 */

export {};
