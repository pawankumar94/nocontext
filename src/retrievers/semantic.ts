/**
 * Embedding retrieval.
 *
 * Present so that stuffing is visible: an index padded with query vocabulary
 * gains lexically and stays flat here. The gap between the two is the signal.
 */

export {};
