/**
 * CorpusSource over documents held in memory.
 *
 * Used by tests, and by any surface that already has the documents and no
 * filesystem to point at, which is the normal case for an MCP server.
 */

export {};

// Not implemented. Contract is in ../core/types.ts.
